apiVersion: v1
kind: ConfigMap
metadata:
  name: gooaye-summary-config
data:
  CRON_EMAILS: "r76021061@gmail.com"
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "465"
  # DB_PATH 已移除：系統自 v3.0.0 起改用 Firebase Firestore，不再使用 SQLite
  # SMTP_USER: "your-email@gmail.com" # 建議使用 Secret
  # SMTP_PASS: "your-app-password"    # 建議使用 Secret
  # GEMINI_API_KEY: "your-api-key"    # 建議使用 Secret
  # INTERNAL_AUTH_USER / INTERNAL_AUTH_PASS 請加入 gooaye-secrets（保護敏感 API 路由）
